{
  "name": "backend-integration-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "yarn prisma generate --schema ./prisma/schema.prisma && yarn prisma db push && yarn prisma db seed && jest --runInBand"
  },
  "prisma": {
    "seed": "yarn ts-node ./prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^3.12.0",
    "@types/bcrypt": "^5.0.0",
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.25",
    "axios": "^0.26.1",
    "bcrypt": "^5.0.1",
    "config": "*",
    "jest": "^27.5.1",
    "prisma": "^3.12.0",
    "shared": "*",
    "ts-jest": "^27.1.4",
    "ts-node": "^10.7.0",
    "tsconfig": "*",
    "typescript": "^4.6.3"
  }
}
