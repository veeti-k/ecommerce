import { Input } from "@chakra-ui/react";
import { FlexDiv } from "../Containers";

export const Search = () => {
  return (
    <FlexDiv fullWidth>
      <Input placeholder="Search" />
    </FlexDiv>
  );
};
