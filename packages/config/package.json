{
  "name": "config",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && ./node_modules/.bin/tsc -p ./tsconfig.json",
    "typings": "rm -rf dist && ./node_modules/.bin/tsc -p ./tsconfig.json",
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf dist"
  },
  "dependencies": {
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "tsconfig": "*"
  }
}
