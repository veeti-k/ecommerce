{
  "name": "@ecommerce/database",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:studio": "npx prisma studio",
    "db:generate-client": "npx prisma generate",
    "build": "yarn db:generate-client",
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^3.15.2"
  },
  "devDependencies": {
    "prisma": "^3.15.2"
  }
}
