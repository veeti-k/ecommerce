{
  "scripts": {
    "dev": "docker-compose -f docker-compose.dev.yml up -d dev-db dev-zinc",
    "devdown": "docker-compose -f docker-compose.dev.yml down",
    "test:backend": "rm -rf ./TEST-DATA && docker-compose -f docker-compose.test.yml up --abort-on-container-exit --build && docker-compose -f docker-compose.test.yml rm -fsv"
  }
}